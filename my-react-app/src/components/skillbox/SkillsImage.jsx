import styled from "styled-components";

const SkillsImage = styled.img`
    overflow: hidden;
    display: block;
    width: 8em;
    height: 8em;
    object-fit: cover;
    z-index: 1;
    margin: auto;`

export default SkillsImage;